/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind v4: theme tokens are defined via @theme in src/app/globals.scss
  // Keep this file minimal unless you need safelists or plugins.
  content: ['./src/**/*.{js,ts,jsx,tsx}'],
  theme: {},
  plugins: [],
};
